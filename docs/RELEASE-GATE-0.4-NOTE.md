# Lifecycle release note

Lifecycle lock and reconciler lock interoperability are now gated separately from the existing smoke suite. Upgrade entrypoints and real backends remain explicit release blockers until verified.
