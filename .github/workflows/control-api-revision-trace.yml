name: Control API revision trace

on:
  push:
    branches: [0.4.x]

permissions:
  contents: read

jobs:
  trace:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run revision trace
        run: bash tests/control-api-revision-trace.sh
